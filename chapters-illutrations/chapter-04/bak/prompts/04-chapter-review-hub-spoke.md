<!-- position: 4.5 小结 / 图 4-4 -->

<!-- layout: hub-spoke -->

请根据提供的输入信息，为《Agent Skills 图解指南》生成一张文章配图。默认中文。请使用 nano banana 生成。

一、插图输入
插图位置：第 4 章 / 4.5 小结 / 图 4-4
本段内容摘要：第 4 章亲手做了三个技能（约束型写作风格、模板型会议纪要、流程型文章配图），掌握了迭代技能的两大原则（一次别改太多、底线用规则偏好加解释）和三步验证法（触发测试、功能测试、对比测试）。
读者要获得的“瞬间理解”：第 4 章的核心是“动手做技能”，围绕它展开三个维度——做什么、怎么改、怎么验
推荐布局：hub-spoke
配图作用：信息补充
主要元素：中心一个醒目的核心节点“动手做技能”，向外辐射连接三个子节点（三种类型、迭代原则、验证方法）
次要元素：每个辐射节点内有子项细节；连接线用黑色细线；适当涂鸦装饰
需要上屏文字：（无）
禁用元素：真实截图、写实人物、复杂纹理、密集文字、英文文字、章节标题

二、图片比例
16:9 横向。

三、全书统一风格
手绘涂鸦风插画——像用马克笔在白板或牛皮纸上随手画的。线条不求笔直，带点手抖感、断续感；字迹带手写感但清晰可读；框线随意弯曲，形状略带不规则；连接线/箭头自然弯曲，不用直角。整体轻松、有温度、不正式，像“朋友在白板上帮你画的图”。干净构图，大量留白；2D；无阴影或极少阴影。人物仅用简笔小人/涂鸦小人，不写实。

四、配色方案（出版三色限定，不可更改）
主色：#D87757（暖赤陶色）—— 主要视觉元素、强调色块。
黑色 —— 轮廓线、文字、屏幕脸、深色区域。
白色 —— 背景、留白、面板、关节点缀。
仅使用以上三色及其深浅变体（通过透明度/灰度过渡），不使用其他色相。

五、布局视觉语法（hub-spoke 辐射图式）
画面中心放置一个醒目的大圆形核心节点（#D87757 填充），向外辐射三条黑色细连接线，分别连接三个均匀分布的辐射节点（较小的圆角矩形）。中心节点明显大于辐射节点，整体呈放射状布局。中心节点外有 #D87757 低透明度光环衬托重要性。

辐射结构：

1. **中心节点：动手做技能**
   * 大圆形，#D87757 填充，内有一个手握工具的简笔涂鸦图标
   * 下方标签：“动手做技能”

2. **辐射节点一（左上方）：三种类型**
   * 圆角矩形节点，#D87757 低透明度填充
   * 节点标签：“三种类型”
   * 节点下方挂三个小标签卡片：
     * 写作风格（约束型）—— 旁有锁头小图标
     * 会议纪要（模板型）—— 旁有表格小图标
     * 文章配图（流程型）—— 旁有流程箭头小图标

3. **辐射节点二（右上方）：迭代原则**
   * 圆角矩形节点，#D87757 中透明度填充
   * 节点标签：“迭代原则”
   * 节点下方挂两个小标签卡片：
     * 一次别改太多 —— 旁有脚印小图标
     * 底线用规则，偏好加解释 —— 旁有天平小图标
   * 旁边有一个小循环箭头涂鸦

4. **辐射节点三（正下方）：三步验证**
   * 圆角矩形节点，#D87757 高透明度填充
   * 节点标签：“三步验证”
   * 节点下方挂三个带编号的小标签卡片：
     * ① 触发测试 —— 旁有闪电小图标
     * ② 功能测试 —— 旁有齿轮小图标
     * ③ 对比测试 —— 旁有对比天平小图标

五点五、信息层次
主要元素（最大最醒目，占画面 60%+）：中心核心节点和三个辐射节点
次要元素（中等大小，围绕/衬托主要元素）：辐射节点下的子项标签卡片、小图标、连接线、涂鸦装饰
视觉权重：中心节点是绝对焦点，三个辐射节点是次级焦点，子项卡片提供细节，合力传达“一个核心、三个维度”的清晰结构。

六、构图要求
中心节点居于画面正中偏上；三个辐射节点呈三角形均匀分布在中心周围；连接线清晰但不粗重；每个辐射节点周围有充足空间展示子项内容；画面不拥挤，保持手绘感和大量留白。

七、文字处理
中心节点标签文字最大最醒目（“动手做技能”），字体使用 Noto Sans CJK SC。辐射节点标签文字中等大小（“三种类型”“迭代原则”“三步验证”）。子项标签文字较小但清晰可读。所有文字使用中文。

八、技术与限制
所有图像与文字都保持手绘/涂鸦感，避免写实元素、照片风、死板直角连线和企业风流程图；不出现品牌 logo；不使用真实界面截图风；线条可以不完美、有断续感；画面简洁突出关键词与核心概念。不出现章节标题或图示标题（如“图 4-4”“第 4 章”等）。

九、最终生成目标
让读者在不读正文的情况下，3 秒看懂：第 4 章围绕“动手做技能”展开了三个维度——做了三种类型的技能、掌握了迭代原则、学会了三步验证法。
