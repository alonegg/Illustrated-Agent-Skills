<!-- position: 5.3.1 子智能体是什么 / 图 5-2 -->

<!-- layout: hub-spoke -->

<!-- reference_image: /Users/jimliu/.claude/image-cache/21246bd0-eb1e-44c0-8634-d72914564c46/3.png -->

请根据提供的输入信息，为《Agent Skills 图解指南》生成一张文章配图。默认中文。请使用 nano banana 生成。

一、插图输入
插图位置：第 5 章 / 5.3.1 子智能体是什么 / 图 5-2
本段内容摘要：主智能体把“同时写三版草稿”的任务分别派给三个子智能体。每个子智能体拥有独立的、全新的上下文窗口，互不干扰地工作。完成后只把文件路径交回主智能体，中间过程全部留在子智能体内部。主智能体的上下文保持干净。
读者要获得的“瞬间理解”：主智能体派任务出去，子智能体各自独立干活，只有结果回来，过程不回来
推荐布局：hub-spoke（参考图的上下流转风格改造）
配图作用：概念具象化
主要元素：主智能体（顶部，带进度条表示上下文）、三个子智能体（底部并排，虚线边框表示隔离，各带全新进度条）、结果汇总区
次要元素：派单箭头、回传箭头、文件路径标注、注解文字
需要上屏文字：（无）
禁用元素：真实截图、写实人物、复杂纹理、英文文字、章节标题

二、图片比例
16:9 横向。

三、全书统一风格
手绘涂鸦风插画——像用马克笔在白板或牛皮纸上随手画的。线条不求笔直，带点手抖感、断续感；字迹带手写感但清晰可读；框线随意弯曲，形状略带不规则；连接线/箭头自然弯曲，不用直角。整体轻松、有温度、不正式，像“朋友在白板上帮你画的图”。干净构图，大量留白；2D；无阴影或极少阴影。人物仅用简笔小人/涂鸦小人，不写实。

四、配色方案（出版三色限定，不可更改）
主色：#D87757（暖赤陶色）—— 主要视觉元素、强调色块。
黑色 —— 轮廓线、文字、屏幕脸、深色区域。
白色 —— 背景、留白、面板、关节点缀。
仅使用以上三色及其深浅变体（通过透明度/灰度过渡），不使用其他色相。

五、布局视觉语法

整体采用上下两层结构（参考附图的视觉语言），上层是主智能体，下层是三个并排的子智能体。

**上层——主智能体（画面上方居中）**

* 一个 #D87757 浅色填充的圆角矩形，内部包含：
  * 左侧：一个简笔小人（主厨/指挥者姿态，双手叉腰或手持指挥棒）
  * 右侧：一个进度条图标，进度条大约 30% 满，表示“上下文窗口保持干净”
* 圆角矩形下方标注“主智能体”
* 进度条旁边用小字手写标注“上下文保持干净”

**中间——派单与回传**

* 从主智能体向下伸出三条弧线箭头（黑色实线），分别指向三个子智能体，表示“派任务”
* 三条箭头汇合处（或箭头起始处附近）用手写体标注“派出任务”
* 从三个子智能体向上各回一条弧线箭头（#D87757 虚线），汇聚到主智能体右侧的一个小结果框
* 结果框是一个小圆角矩形，内有漏斗图标 + 三行短横线（表示压缩后的结果列表），旁边标注“只收到文件路径”

**下层——三个子智能体（画面下方并排）**
三个子智能体并排排列，间距均匀，每个用虚线边框的圆角矩形表示（虚线传达“隔离/独立”），内部结构相同：

子智能体 A（左）：

* 虚线圆角矩形，内部浅灰底色
* 顶部标签：“子智能体 A”
* 内部元素：一个全新的进度条（几乎空的，表示“全新上下文窗口”）+ 一个小笔图标（正在写作）+ 几行波浪线（表示中间过程）
* 底部标注文件名：“draft-a.md”

子智能体 B（中）：

* 结构同上
* 标签：“子智能体 B”
* 底部标注：“draft-b.md”

子智能体 C（右）：

* 结构同上
* 标签：“子智能体 C”
* 底部标注：“draft-c.md”

**关键注解文字（手写风格，散落在画面适当位置）**

* 三个子智能体下方统一标注：“各自独立，互不干扰”
* 子智能体区域旁边标注：“全新上下文窗口”（指向进度条）
* 回传箭头旁标注：“中间过程留在内部，只交文件路径”

五点五、信息层次
主要元素（最大最醒目，占画面 60%+）：主智能体圆角矩形 + 三个子智能体虚线矩形
次要元素（中等大小，围绕/衬托主要元素）：派单/回传箭头、结果框、注解文字、进度条
视觉权重：上层主智能体是视觉起点，目光自然向下流向三个子智能体，再沿回传箭头回到结果框。形成“派出去 → 独立工作 → 收结果”的视觉叙事。

六、构图要求
上下两层清晰分隔；三个子智能体大小一致、并排等距，强调平等并行；画面不拥挤，组件之间有呼吸空间；进度条是关键视觉符号（满 vs 空，对比主智能体与子智能体的上下文状态）。

七、文字处理
“主智能体”标签大而醒目；三个子智能体标签清晰可读；文件名（draft-a.md / draft-b.md / draft-c.md）用稍小的手写体；注解文字用最小号但仍可读的手写体。字体使用 Noto Sans CJK SC。所有文字使用中文（文件名除外，文件名保留英文因为是代码产物）。

八、技术与限制
所有图像与文字都保持手绘/涂鸦感，避免写实元素、照片风、死板直角连线和企业风流程图；不出现品牌 logo；不使用真实界面截图风；线条可以不完美、有断续感；画面简洁突出关键词与核心概念。不出现章节标题或图示标题（如“图 5-2”“第 5 章”等）。

九、最终生成目标
让读者在不读正文的情况下，3 秒看懂：主智能体在上方指挥，把任务派给下方三个独立的子智能体（各有全新的上下文窗口），它们各自干活互不干扰，完成后只把文件路径交回来，主智能体的上下文保持干净。
