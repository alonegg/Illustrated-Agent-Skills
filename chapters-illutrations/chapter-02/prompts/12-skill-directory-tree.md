<!-- position: 2.3.2 按需加载：用到什么才拿什么 / 技能目录结构代码块之后 -->

<!-- layout: tree-branching -->

请根据提供的输入信息，为《Agent Skills 图解指南》生成一张文章配图。默认中文。请使用 nano banana 生成。

一、插图输入
插图位置：第 2 章 - 按需加载 - 技能文件目录结构
本段内容摘要：技能包是一个文件夹。最核心的是 SKILL.md（必填），复杂技能还可以附带 scripts/（脚本目录）、references/（参考文档目录）、assets/（静态资源目录）。三个子目录都是可选的，按需添加。
读者要获得的“瞬间理解”：技能包 = SKILL.md（必填）+ 三个可选目录。
推荐布局：tree-branching
配图作用：信息补充
主要元素：根节点是一个大文件夹图标（标注“技能文件夹”），向下分叉出 4 个子节点：SKILL.md（用高亮显示，标注“必填”）、scripts/ 文件夹（标注“脚本，可选”）、references/ 文件夹（标注“参考文档，可选”）、assets/ 文件夹（标注“静态资源，可选”）
次要元素：SKILL.md 节点旁有醒目的星号或勾选标记强调“必填”；三个可选目录节点颜色较浅，旁边有“可选”小标签；每个可选目录下方可有 1 个小文件图标示例（如 scripts/ 下有一个 .py 文件图标）
需要上屏文字：
禁用元素：真实截图、写实人物、复杂纹理、密集文字、代码行

二、图片比例
16:9 横向。

三、全书统一风格
手绘涂鸦风插画——像用马克笔在白板或牛皮纸上随手画的。线条不求笔直，带点手抖感、断续感；字迹带手写感但清晰可读；框线随意弯曲，形状略带不规则；连接线/箭头自然弯曲，不用直角。整体轻松、有温度、不正式，像“朋友在白板上帮你画的图”。干净构图，大量留白；2D；无阴影或极少阴影。人物仅用简笔小人/涂鸦小人，不写实。

四、配色方案（出版三色限定，不可更改）
主色：#D87757（暖赤陶色）—— 主要视觉元素、强调色块。
黑色 —— 轮廓线、文字、屏幕脸、深色区域。
白色 —— 背景、留白、面板、关节点缀。
仅使用以上三色及其深浅变体（通过透明度/灰度过渡），不使用其他色相。

五、布局视觉语法（tree-branching 树形图）

* 顶部根节点为技能文件夹，使用 #D87757 高饱和大圆角矩形，内含文件夹图标
* 根节点向下分出 4 条分支，连接 4 个子节点
* SKILL.md 子节点用 #D87757 中等饱和度，突出“必填”身份，旁边配星号/勾选标记
* 三个可选目录子节点用 #D87757 低透明度（较浅），体现“可选”的次要性
* 连接线为黑色细线，呈直角或斜线
* 可选目录下再延伸出 1 个更小的叶节点（示例文件图标）

五点五、信息层次
主要元素（最大最醒目，占画面 60%+）：根节点大文件夹 + SKILL.md 节点（高亮必填）
次要元素（中等大小，围绕/衬托主要元素）：三个可选目录节点 + 示例文件图标 + 各节点标签
视觉权重：主要元素是视觉焦点，次要元素提供上下文支撑，两者合力传达“技能包结构：一个必填 + 三个可选”。

六、构图要求
根节点居中偏上，树形向下展开；层级之间间距适当；留白充足；元素符号化、边界清晰。

七、文字处理
根节点标签“技能文件夹”。子节点标签：SKILL.md（旁边小字“必填”）、scripts/（旁边小字“可选”）、references/（旁边小字“可选”）、assets/（旁边小字“可选”）。字体使用 Noto Sans CJK SC。文件名使用英文（SKILL.md、scripts/、references/、assets/ 为专有技术名称），说明标签使用中文。

八、技术与限制
所有图像与文字都保持手绘/涂鸦感，避免写实元素、照片风、死板直角连线和企业风流程图；不出现品牌 logo；不使用真实界面截图风；线条可以不完美、有断续感；画面简洁突出关键词与核心概念。

九、最终生成目标
让读者在不读正文的情况下，3 秒看懂：技能包的文件结构——SKILL.md 是必填核心，其余三个目录按需添加。
