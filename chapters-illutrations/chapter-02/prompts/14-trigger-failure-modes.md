<!-- position: 2.4.2 为什么有时候会选错 / "工具调用惰性"段落之后 -->

<!-- layout: binary-comparison -->

请根据提供的输入信息，为《Agent Skills 图解指南》生成一张文章配图。默认中文。请使用 nano banana 生成。

一、插图输入
插图位置：第 2 章 - 为什么有时候会选错 - 误触发 vs 漏触发
本段内容摘要：description 写不好会带来两种代价：误触发（智能体被模糊描述误导，加载了不相关的技能，白白浪费一步，还无端占用上下文窗口）和漏触发（明明装了技能，智能体却没认出来，自己“即兴发挥”干活，结果不稳定）。
读者要获得的“瞬间理解”：错误选了别人的菜谱 vs 明明有菜谱却没用。
推荐布局：binary-comparison
配图作用：概念具象化
主要元素：左侧“误触发”——涂鸦小人端着一个大叉号标记的菜谱（错误技能），表情困惑，旁边有“白白浪费！”气泡；右侧“漏触发”——同一个涂鸦小人站在一个完好的菜谱旁边（菜谱落灰、蜘蛛网），但小人完全无视菜谱，自己在乱炒（手势混乱、烟雾），菜谱旁有“视而不见”标注
次要元素：左侧背景隐约有一个正确的菜谱被忽视；右侧锅里冒出混乱的烟代表“即兴发挥”结果不稳定
需要上屏文字：
禁用元素：真实截图、写实人物、复杂纹理、密集文字

二、图片比例
16:9 横向。

三、全书统一风格
手绘涂鸦风插画——像用马克笔在白板或牛皮纸上随手画的。线条不求笔直，带点手抖感、断续感；字迹带手写感但清晰可读；框线随意弯曲，形状略带不规则；连接线/箭头自然弯曲，不用直角。整体轻松、有温度、不正式，像“朋友在白板上帮你画的图”。干净构图，大量留白；2D；无阴影或极少阴影。人物仅用简笔小人/涂鸦小人，不写实。

四、配色方案（出版三色限定，不可更改）
主色：#D87757（暖赤陶色）—— 主要视觉元素、强调色块。
黑色 —— 轮廓线、文字、屏幕脸、深色区域。
白色 —— 背景、留白、面板、关节点缀。
仅使用以上三色及其深浅变体（通过透明度/灰度过渡），不使用其他色相。

五、布局视觉语法（binary-comparison 对比图）
画面左右分栏，中间有黑色细分隔线。

* 左半（误触发）：黑色低透明度（浅灰调），小人拿着错误菜谱，有凌乱感
* 右半（漏触发）：黑色低透明度（浅灰调），小人无视正确菜谱自己乱搞，有混乱感
* 两侧都是“错误”状态，不存在“正确”一侧，用不同错误类型的视觉符号区分
* 整体保持极简扁平矢量风格
* 左右各一个短标签标明错误类型

五点五、信息层次
主要元素（最大最醒目，占画面 60%+）：左侧小人 + 带叉号的错误菜谱；右侧小人 + 落灰的正确菜谱（小人无视它）
次要元素（中等大小，围绕/衬托主要元素）：左侧“白白浪费”气泡；右侧锅中混乱烟雾 + “即兴发挥”标注
视觉权重：主要元素是视觉焦点，次要元素提供上下文支撑，两者合力传达“误触发 vs 漏触发两种代价”。

六、构图要求
主视觉元素居中或按布局需要排列；信息密度高但不拥挤；留白用于呼吸；元素符号化、边界清晰；避免细碎装饰。
左侧标签“误触发”，右侧标签“漏触发”。

七、文字处理
左侧标签“误触发”，气泡内写“白白浪费！”。右侧标签“漏触发”，菜谱旁标注“视而不见”，锅旁标注“即兴发挥”。字体使用 Noto Sans CJK SC。所有文字使用中文。

八、技术与限制
所有图像与文字都保持手绘/涂鸦感，避免写实元素、照片风、死板直角连线和企业风流程图；不出现品牌 logo；不使用真实界面截图风；线条可以不完美、有断续感；画面简洁突出关键词与核心概念。

九、最终生成目标
让读者在不读正文的情况下，3 秒看懂：description 写不好导致两种失败——错误地加载不相关技能（误触发），或完全不触发技能（漏触发）。
