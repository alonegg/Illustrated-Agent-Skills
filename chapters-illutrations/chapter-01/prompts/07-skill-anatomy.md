<!-- position: 第 1 章 / 1.3.2 SKILL.md 的三个部分 / 段落结束后 -->

<!-- layout: structural-breakdown -->

请根据提供的输入信息，为《Agent Skills 图解指南》生成一张文章配图。默认中文。请使用 nano banana pro 生成。

一、插图输入
插图位置：第 1 章 / 1.3.2 SKILL.md 的三个部分 / “名片负责快速判断该不该用，正文告诉它具体怎么执行”段落后
本段内容摘要：SKILL.md 由三个部分组成——name（技能名称/ID）、description（简介，智能体靠它判断该不该调用）、正文（详细的执行步骤和规则）。前两项在 YAML frontmatter 里，相当于技能的“名片”；正文在第二个 --- 之后。智能体启动时只看名片，决定调用后才读正文。
读者要获得的“瞬间理解”：SKILL.md 就三块——名字、简介、正文。名片负责被发现，正文负责被执行。
推荐布局：structural-breakdown
配图作用：信息补充

主要元素：
（1）画面中心偏左：一个大的手绘文件图标，文件顶部写着“SKILL.md”。文件内部被两条手绘水平虚线分成三个区域，从上到下分别用不同深浅的底色区分：

* 顶部区域（最浅）：写着“name: weekly-report”
* 中部区域（中等深浅）：写着“description: 生成周报…”
* 底部区域（最深，占文件面积最大）：写着几行简略的步骤文字符号（用波浪线代替具体文字，暗示“详细内容”）
  （2）从文件的三个区域分别引出标注线，指向右侧的三个标签卡片：
* 标签 1：“名称”，旁注“技能的 ID”
* 标签 2：“简介”，旁注“什么时候用”
* 标签 3：“正文”，旁注“怎么一步步执行”
  （3）文件上半部分（name + description）用一个手绘大括号圈起来，旁边写“名片”，一个简笔小人快速扫一眼（表示“启动时只看名片”）

次要元素：
（1）标注线用黑色细线，起始处有小圆点
（2）标签卡片用 #D87757 不同透明度区分（从浅到深对应三个部分）
（3）“名片”旁边画一个眼睛图标，暗示“快速扫描”
（4）底部正文区域旁画一个展开/阅读图标，暗示“需要时才细读”
（5）适当散落小装饰：小箭头、小星星

需要上屏文字：（无）
禁用元素：真实截图、写实人物、复杂纹理、品牌 logo、密集英文、死板直角连线、企业风流程图

二、图片比例
16:9 横向。

三、风格
手绘涂鸦风插画——像用马克笔在白板或牛皮纸上随手画的。线条不求笔直，带点手抖感；字迹带手写感但清晰可读；整体轻松、有温度、不正式。

四、配色方案（出版三色限定，不可更改）
主色：#D87757（暖赤陶色）—— 标签卡片填充、文件区域底色渐变、大括号“名片”标注。
黑色 —— 文件轮廓线、标注线、文字、小人轮廓。
白色 —— 背景、留白。
仅使用以上三色及其深浅变体（通过透明度/灰度过渡），不使用其他色相。

五、布局视觉语法

* 主体对象（SKILL.md 文件）居中偏左，占画面主要空间
* 从主体的各部分引出标注线，指向右侧的标签说明
* 标注线用细实线 + 小圆点起始
* 各部分用不同深浅色块区分
* 整体像技术图纸的标注式说明，但保持涂鸦插画风格

六、构图要求
文件图标占画面左侧约 40% 宽度，高度占 60%；右侧标签卡片整齐排列但不死板；标注线不交叉重叠；“名片”大括号在文件右侧偏上方；整体留白充足，不拥挤。

七、文字处理
所有文字带手写感，使用 Noto Sans CJK SC；文件内的代码文字（name、description）用等宽手写感；标签文字（“名称”“简介”“正文”）清晰醒目；旁注小一号。所有文字使用中文（name 和 description 字段名保留英文，因为这是代码）。

八、技术与限制
保持手绘/涂鸦感，避免任何写实或照片风元素；不出现品牌 logo；线条可以不完美、有断续感；整体气质像“朋友在白板上帮你拆解一个文件的结构”。

九、最终生成目标
让读者 3 秒看懂：SKILL.md 由三部分组成——名称、简介（合称“名片”，启动时扫描）和正文（调用时才读）。结构清晰，一目了然。
